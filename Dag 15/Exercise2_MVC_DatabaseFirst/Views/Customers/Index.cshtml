@model IEnumerable<Exercise2_MVC_DatabaseFirst.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cEmail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cPhone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cPhone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cID }) |
            @Html.ActionLink("Details", "Details", new { id=item.cID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cID })
        </td>
    </tr>
}

</table>
